#include <stdio.h>
int main)
{
     printf"HEllO");
     return 0;
     }
     
     
     
     
     现在是linux操作:CD Desktop
     touch 1-1.c(创建名为1-1的.c文件)
     gedit 1-1.c（编辑)
     再输入上述代码，保存
     然后输入gcc -0 1-1 1-1.c(编译命令)
     结果输出  HELLO
   
   
   gedit hello_shell.sh
   
   #!/bin/bash
   for ((i=0;i<10;i++));do
       echo"hello shell"
    done
    
chmod 755 hello_shell.sh
./hello_shell.sh


添加自定义路径到“PATH”环境变量
PATH=$PATH:/home/shiyanlou/mybin(这只能是临时添加，退出终端就会消失)
永久添加：  echo "PATH=$PATH:/home/shiyanlou/mybin" >> .zshrc（zshrc只是实验中的配置文件。相应Bash的配置文件為.bashrc)

echo $PATH(输出绝对路径)
